<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.shop.mapper.DataNewsMarkMapper">
    
    <resultMap type="DataNewsMark" id="DataNewsMarkResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="remark"    column="remark"    />
        <result property="sort"    column="sort"    />
        <result property="status"    column="status"    />
        <result property="deleted"    column="deleted"    />
        <result property="createAt"    column="create_at"    />
    </resultMap>

    <sql id="selectDataNewsMarkVo">
        select id, name, remark, sort, status, deleted, create_at from data_news_mark
    </sql>

    <select id="selectDataNewsMarkList" parameterType="DataNewsMark" resultMap="DataNewsMarkResult">
        <include refid="selectDataNewsMarkVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="sort != null "> and sort = #{sort}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="deleted != null "> and deleted = #{deleted}</if>
            <if test="createAt != null "> and create_at = #{createAt}</if>
        </where>
    </select>
    
    <select id="selectDataNewsMarkById" parameterType="Long" resultMap="DataNewsMarkResult">
        <include refid="selectDataNewsMarkVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertDataNewsMark" parameterType="DataNewsMark" useGeneratedKeys="true" keyProperty="id">
        insert into data_news_mark
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="remark != null">remark,</if>
            <if test="sort != null">sort,</if>
            <if test="status != null">status,</if>
            <if test="deleted != null">deleted,</if>
            <if test="createAt != null">create_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="remark != null">#{remark},</if>
            <if test="sort != null">#{sort},</if>
            <if test="status != null">#{status},</if>
            <if test="deleted != null">#{deleted},</if>
            <if test="createAt != null">#{createAt},</if>
         </trim>
    </insert>

    <update id="updateDataNewsMark" parameterType="DataNewsMark">
        update data_news_mark
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            <if test="createAt != null">create_at = #{createAt},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteDataNewsMarkById" parameterType="Long">
        delete from data_news_mark where id = #{id}
    </delete>

    <delete id="deleteDataNewsMarkByIds" parameterType="String">
        delete from data_news_mark where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
</mapper>